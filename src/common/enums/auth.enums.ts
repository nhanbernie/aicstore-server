export const ROLE = {
  USER: 'user',
  ADMIN: 'admin',
  MODERATOR: 'moderator',
};

export enum MESSAGE_ROLE {
  USER = 'user',
  ASSISTANT = 'assistant',
  SYSTEM = 'system',
}

export enum CONTENT_TYPE {
  TEXT = 'text',
  IMAGE_URL = 'image_url',
}
